// Factorial of given number using function

#include<stdio.h>
int fact(int n)
{
    int fact=1,i;
    for(i=1;i<=n;i++)
    {
        fact=fact*i;
    }
    return fact;
}
int main()
{
    int n,ans;
    scanf("%d",&n);
    ans=fact(n);
    printf("%d",ans);
}
