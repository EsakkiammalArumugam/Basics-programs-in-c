// LCM of two numbers

#include<stdio.h>
int main()
{
    int a,b,i,ans,lcm;
    scanf("%d %d",&a,&b);
    for(i=1;i<=a,i<=b;i++)
    {
        if(a%i==0 && b%i==0)
        {
            ans=i;
        }
    }
    lcm=a*b/ans;
    printf("%d",lcm);
}
