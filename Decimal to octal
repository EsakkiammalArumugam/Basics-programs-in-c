//  Decimal to octal

#include<stdio.h>
void binary(int n)
{
    if(n==0)
      return;
    else
    {
        binary(n/8);
        printf("%d",n%8);
    }
}
int main()
{
    int n,i;
    scanf("%d",&n);
    binary(n);
}
