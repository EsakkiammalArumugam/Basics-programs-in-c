// Palindrome

#include<stdio.h>
int main()
{
    int n,temp,ans=0,dig;
    scanf("%d",&n);
    temp=n;
    while(n)
    {
        dig=n%10;
        ans=ans*10+dig;
        n=n/10;
    }
    if(ans==temp)
    {
        printf("palindrome");
    }
    else
    {
        printf("not  palindrome");
    }
}
