// Largest prime factor

#include<stdio.h>
int main()
{
    int n,i=2;
    scanf("%d",&n);
    while(n!=1)
    {
        while(n%i==0)
        {
            n=n/i;
        }
        i++;
    }
    printf("%d",i-1);
}
